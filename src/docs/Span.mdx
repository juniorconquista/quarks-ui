---
name: Span
menu: Typography
---

import { Playground, Props } from 'docz';
import { Span } from '../ds/typography';

# Span

The _Span_ tag is an inline container used to mark up a part of a text, or a part of a document.

### Best practices

-   The _Span_ tag is easily styled by CSS or manipulated with JavaScript using the class or id attribute.
-   The _Span_ tag is much like the _div_ element, but _div_ is a block-level element and _Span_ is an inline element.

## Properties

<Props of={Span} />

## Basic usage

<Playground>
    <Span styling="base">Base</Span>
</Playground>

## Customization

### Color

The color utility parses a component's _color_ and _bg_ props and converts them into CSS declarations. By default the raw value of the prop is returned. Color palettes can be configured with the ThemeProvider to use keys as prop values, with support for dot notation.

You can see all the color customization [here](https://styled-system.com/api/#color).

```javascript
// picks the value defined in `theme.colors.blue`
<Span styling="base" color='blue'>Title</Span>

// picks up a nested color value using dot notation `theme.colors.gray[0]`
<Span color='gray.0'>Title</Span>

// raw CSS color value
<Span color='#f00'>Title</Span>

// background colors
<Span bg='blue'>Title</Span>

// verbose prop
<Span backgroundColor='blue'>Title</Span>
```

### Space

The space utility converts shorthand margin and padding props to _margin_ and _padding_ CSS declarations.

You can see all the space customization [here](https://styled-system.com/api#space).

```javascript
// sets margin value of `theme.space[2]`
<Span styling="base" m={2}>Title</Span>

// sets a margin value of `16px` since it's greater than `theme.space.length`
<Span m={16}>Title</Span>

// sets margin `'auto'`
<Span m='auto'>Title</Span>

// sets margin `8px` fom mobile devices, `16px` for tablet and `32px` for desktop from the first breakpoint and up
<Span m={{ mobile:2, tablet:3, desktop:4 }}>Title</Span>
```

### Typography

Typography props include _fontFamily_, _fontSize_, _fontWeight_, _lineHeight_, _letterSpacing_, _textAlign_, and _fontStyle_.

You can see all the typography customization [here](https://styled-system.com/api/#typography).

```javascript
// font-size of `theme.fontSizes[3]`
<Span styling="base" fontSize={3}>Title</Span>

// font-size `32px`
<Span fontSize={32}>Title</Span>

// font-size `'2em'`
<Span fontSize='2em'>Title</Span>

// font-size `10px` on all viewports and `12px` from the first breakpoint and up
<Span fontSize={{ mobile:10, desktop:12 }}>Title</Span>

// fontFamily
<Span fontFamily='mono'>Title</Span>

// textAlign
<Span textAlign='center'>Title</Span>
<Span textAlign={[ 'center', 'left' ]}>Title</Span>

// lineHeight
<Span lineHeight='1.25'>Title</Span>

// fontWeight
<Span fontWeight='bold'>Title</Span>

// letterSpacing
<Span letterSpacing='0.1em'>Title</Span>
```
